# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=parking_zone_db
DB_USER=root
DB_PASSWORD='Os51t=Ag/3=B'


# Database Configuration
DB_HOST=db30842.public.databaseasp.net
DB_PORT=3306
DB_NAME=db30842
DB_USER=db30842
DB_PASSWORD='3d+PcK6=Z2-z'           # OK así para node-mysql/knex/sequelize (no URL)

# Si vas a usar URL (tipo Sequelize/Prisma), usa la versión URL-encoded:
DB_URL=mysql://db30842:3d%2BPcK6%3DZ2-z@db30842.public.databaseasp.net:3306/db30842?sslmode=require


# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here
JWT_EXPIRES_IN=24h

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# File Upload Configuration
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=5242880

# LPR Configuration
LPR_MODEL_PATH=./models
CAMERA_INDEX=0

# Rekor/OpenALPR API Configuration
# IMPORTANTE: La API key debe corresponder al servicio que uses:
# - Para Rekor Scout/OpenALPR: usa https://api.openalpr.com/v2/recognize (secret_key como query param)
# - Para Rekor CarCheck: usa https://api.rekor.ai/v1/carcheck (API key en header Authorization)
# Obtén tu API key desde: https://cloud.openalpr.com/settings
REKOR_API_KEY=7f2129c4debb5c98306565da3be8ea36bff7fa43
# Endpoint de la API (api.openalpr.com es el correcto para la API, cloud.openalpr.com es solo la interfaz web)
REKOR_API_URL=https://api.openalpr.com/v2/recognize
# Company ID de Rekor (necesario si tienes habilitada la autenticación por Company ID)
# Lo encuentras en: https://cloud.openalpr.com/settings (Información de la compañía)
REKOR_COMPANY_ID=2c3f3ffa-5871-4d89-89d7-ca19e02a5536

# SMTP (Email) Configuration
# For Gmail, create an App Password (Google Account -> Security -> 2-Step Verification -> App Passwords)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ruedanegociosbeni@gmail.com
SMTP_PASS=pqsq rhei hjmx nhtm
# Optional: customize the From header. Defaults to SMTP_USER if not set
SMTP_FROM="ParkingZone <no-reply@yourdomain.com>"

DB_SSL=true